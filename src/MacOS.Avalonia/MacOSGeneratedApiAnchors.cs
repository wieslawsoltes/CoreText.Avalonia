using AppKit;
using CoreAnimation;
using Foundation;

namespace MacOS.Avalonia;

internal static class MacOSGeneratedApiAnchors
{
    public static Type[] RequiredTypes { get; } =
    [
        typeof(NSApplication),
        typeof(NSWindow),
        typeof(NSView),
        typeof(NSOpenGLContext),
        typeof(NSOpenGLPixelFormat),
        typeof(CAMetalLayer),
        typeof(NSProcessInfo)
    ];
}